import type { RowValue, RowResult } from '../Row';

export type Matrix = [RowValue, RowValue, RowValue, RowValue, RowValue];
export type ResultMatrix = [RowResult, RowResult, RowResult, RowResult, RowResult];
